import { Camera } from ".pnpm/@types+three@0.176.0/node_modules/@types/three/src/cameras/Camera.js";
import { Material } from ".pnpm/@types+three@0.176.0/node_modules/@types/three/src/materials/Material.js";
import { Plane } from ".pnpm/@types+three@0.176.0/node_modules/@types/three/src/math/Plane.js";
import { WebGLProperties } from ".pnpm/@types+three@0.176.0/node_modules/@types/three/src/renderers/webgl/WebGLProperties.js";

export class WebGLClipping {
    constructor(properties: WebGLProperties);

    uniform: { value: any; needsUpdate: boolean };

    /**
     * @default 0
     */
    numPlanes: number;

    /**
     * @default 0
     */
    numIntersection: number;

    init(planes: any[], enableLocalClipping: boolean): boolean;
    beginShadows(): void;
    endShadows(): void;
    setGlobalState(planes: Plane[], camera: Camera): void;
    setState(material: Material, camera: Camera, useCache: boolean): void;
}
