import {
    MagnificationTextureFilter,
    Mapping,
    MinificationTextureFilter,
    PixelFormat,
    TextureDataType,
    Wrapping,
} from ".pnpm/@types+three@0.176.0/node_modules/@types/three/src/constants.js";
import { VideoTexture } from ".pnpm/@types+three@0.176.0/node_modules/@types/three/src/textures/VideoTexture.js";

declare class VideoFrameTexture extends VideoTexture {
    constructor(
        mapping?: Mapping,
        wrapS?: Wrapping,
        wrapT?: Wrapping,
        magFilter?: MagnificationTextureFilter,
        minFilter?: MinificationTextureFilter,
        format?: PixelFormat,
        type?: TextureDataType,
        anisotropy?: number,
    );

    // FIXME Replace with VideoFrame when we no longer need to support TypeScript 5.0
    setFrame(frame: unknown): void;
}

export { VideoFrameTexture };
